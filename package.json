{
  "name": "2606-authikey-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler pages dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "astro build && wrangler pages deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "astro": "^6.4.6"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "wrangler": "^4.99.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
